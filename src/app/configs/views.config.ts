/**
 * View identifiers for the extension.
 * Used for TreeView and Webview panel registration.
 */
export enum ViewIds {
  JsonView = 'jsonView',
  FeedbackView = 'feedbackView',
  FilesView = 'filesView',
}

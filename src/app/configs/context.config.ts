/**
 * Context key identifiers for the extension.
 * Used with setContext to control command visibility and enablement.
 */
export enum ContextKeys {
  IsSplitView = 'isSplitView',
  LiveSyncEnabled = 'liveSyncEnabled',
}

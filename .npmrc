# ------------------------------------------------------
# pnpm configuration for this repository
# ------------------------------------------------------
# This file enforces consistent dependency installation
# and predictable script behavior across environments.
#
# It must be committed to the repository.
# ------------------------------------------------------

# ------------------------------------------------------
# Lockfile enforcement
# ------------------------------------------------------
# Require pnpm-lock.yaml to be in sync with package.json.
# Installation fails if there is any mismatch.
prefer-frozen-lockfile=true

# ------------------------------------------------------
# node_modules layout
# ------------------------------------------------------
# Use a hoisted layout (similar to npm).
# This improves compatibility with common tooling.
node-linker=hoisted

# ------------------------------------------------------
# Hoisting behavior
# ------------------------------------------------------
# Avoid aggressive hoisting of dependencies.
# Packages should declare everything they use explicitly.
shamefully-hoist=false

# ------------------------------------------------------
# Workspace behavior
# ------------------------------------------------------
# Prefer local workspace packages when available.
link-workspace-packages=true

# ------------------------------------------------------
# Script execution behavior
# ------------------------------------------------------
# Allow normal lifecycle scripts (install, prepare, postinstall).
# Disable automatic execution of preX/postX script wrappers.
#
# Example:
# Running "pnpm run build" will NOT automatically run
# "prebuild" or "postbuild".
enable-pre-post-scripts=false

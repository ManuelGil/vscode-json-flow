# ------------------------------------------------------
# Project environment configuration
# ------------------------------------------------------
# This file defines REQUIRED environment variables
# for local development of this project.
#
# These variables are part of the repository contract.
# ------------------------------------------------------

# VS Code extension runtime flags
export VSCODE_EXTENSION=true
export EXTENSION_MODE=development

# Load Node.js version from .nvmrc (via direnv)
layout node
